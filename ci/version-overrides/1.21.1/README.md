This directory stores source and build files applied when generating the 1.21.1 branch.

Minecraft 1.21.1 starts NeoForge support and switches common to NeoForm via ModDevGradle.

This is also the only current Sable integration recipe. Keep Sable Companion dependencies and `SableCompanion` imports in this override unless another matrix entry is explicitly verified against Sable.
